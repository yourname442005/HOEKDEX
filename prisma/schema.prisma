// Hoekdex Database Schema
// Phase 0: Minimal scaffold to validate Prisma + Neon connection
// Phase 1 will add all entities (User, Person, ClaimedActivity, etc.)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
